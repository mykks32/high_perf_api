config:
  target: "http://localhost:8000"
  phases:
    - duration: 10
      arrivalRate: 200
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "Get Orders Concurrent Test"
    flow:
      - get:
          url: "/api/orders?page=1&limit=10"